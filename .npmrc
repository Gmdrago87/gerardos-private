# npm configuration
# Reduce audit level to moderate to avoid blocking on low-severity issues
audit-level=moderate

# Use npm's default registry
registry=https://registry.npmjs.org/

# Cache configuration
cache=/tmp/npm-cache
cache-lock-stale=604800
cache-lock-wait=10000
